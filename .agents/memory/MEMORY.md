- [PropOS CRM stack decisions](propos-stack.md) — key constraints: customFetch returns parsed body (use apiFetch for raw Response); notification schema uses titleEn/bodyEn columns; permissions fallback to DEFAULT_ROLE_PERMISSIONS
- [Mobile auth pattern](mobile-auth.md) — Expo mobile uses AsyncStorage for JWT, setBaseUrl called outside components in _layout.tsx; AuthContext does cookie+bearer header for /auth/me
- [Supabase SSL connection](supabase-ssl.md) — pg v8+ treats sslmode=require as verify-full; must strip sslmode from URL and use ssl:{rejectUnauthorized:false} in Pool config; DATABASE_URL is Replit-managed, use SUPABASE_DATABASE_URL instead
- [PropOS feature completions](propos-features.md) — resale_photos table + social links (instagramUrl/facebookUrl/whatsappNumber) added to DB; apiFetch for /reports/resale (not in Orval client); Framer Variants `type` must be `"spring" as const`; implicit `any` params from API client happen when lib dist not built — suppress with eslint-disable
