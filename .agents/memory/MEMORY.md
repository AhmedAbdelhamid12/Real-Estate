- [PropOS CRM stack decisions](propos-stack.md) — key constraints: customFetch returns parsed body (use apiFetch for raw Response); notification schema uses titleEn/bodyEn columns; permissions fallback to DEFAULT_ROLE_PERMISSIONS; CRM vite.config needs proxy /api→localhost:8080; @workspace/db seed requires @workspace/permissions + bcryptjs as deps; run seed via pnpm --filter @workspace/db run seed
- [PropOS feature completions](propos-features.md) — mobile auth screens, language switcher (LanguageContext in mobile/contexts), web i18n all 8 pages, RTL fix, projectsPage imageUrl field, resalePage AddPhotoDialog
- [Editorial design system](editorial-design.md) — Editorial palette is now live in darkColors; gold-tinted borders; CSS grid texture via .dark body background-image; mobile fixes: use c.sidebarActiveFg not hardcoded #0A1E38; dark mode forced globally: web via `document.documentElement.classList.add("dark")` in main.tsx, mobile via useColors always returning buildNativeTheme(true); shared AuthShell component at artifacts/crm/src/components/layout/AuthShell.tsx wraps all auth pages
- [Mobile auth pattern](mobile-auth.md) — Expo mobile uses AsyncStorage for JWT, setBaseUrl called outside components in _layout.tsx; AuthContext does cookie+bearer header for /auth/me
- [Supabase SSL connection](supabase-ssl.md) — pg v8+ treats sslmode=require as verify-full; must strip sslmode from URL and use ssl:{rejectUnauthorized:false} in Pool config; DATABASE_URL is Replit-managed, use SUPABASE_DATABASE_URL instead
- [Design tokens shared package](design-tokens-setup.md) — lib/design-tokens is the single source of truth for TIL brand; web uses Vite aliases (fs.strict:false); mobile resolves via Metro; use bare imports (no .js extension) in package src files
- [Mobile auth token fix](mobile-auth-token.md) — login route must return `{ user, token, accessToken: token }` in response body; mobile AsyncStorage stores it; Bearer header used for /auth/me since cookies don't work cross-origin on Expo web
